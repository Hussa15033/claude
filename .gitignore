# Runtime data produced inside the container (not source)
/dtldata/
*.log

# Editor / OS noise
.DS_Store
*.swp
.claude
local_llm_test

# Local deployment config (copy from .env.example). Keep .env.example tracked.
.env